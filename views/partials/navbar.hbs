<nav class="navbar navbar-dark bg-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    {{#if user._id}}
    <a class="navbar-brand" href="/"><span id="logged-in-user">{{{user.nickname}}}</span>'s Bloodhound</a>
    {{else}}
    <a class="navbar-brand" href="/">Bloodhound</a>
    {{/if}}
    <div id="navbar" class="nav navbar-nav">
      {{#if user._id}}
      <a class="nav-item nav-link" href="/collection">Collection</a>
      {{/if}}
    </div>

    <div class="nav navbar-nav pull-right">
      {{#if user._id}}
      <a href="/settings" class="nav-item settings-button">Settings</a>
      {{else}}
      <a href="/login" class="nav-item settings-button">Login</a>
      {{/if}}
    </div>
  </div>
</nav>
